Here is your clean, professional, ready-to-paste README.md question set exactly as requested.

You can copy it directly into your GitHub repo as-is.

⸻


# 📘 Phase 1 – JavaScript Basics: Question Set

This question set covers all concepts from **Phase 1**, including:

- Variables & Data Types  
- Operators  
- Loops (`for…of`, `for…in`)  
- Functions  
- Arrays & Array Methods  

Use this as homework, revision, or assignments.

---

## ⭐ 1. Variables & Data Types (10 Questions)

### **Q1.** What is the difference between `var`, `let`, and `const`?

### **Q2.** What are primitive data types in JavaScript? List all of them.

### **Q3.** What are non-primitive (reference) types? Give examples.

### **Q4.** What will be the output? Why?
```js
console.log("10" == 10);
console.log("10" === 10);

Q5. What is undefined and how is it different from null?

Q6. Explain why correct data types are important in Node.js.

Q7. Predict the output:

let x = 5;
x = "five";
console.log(typeof x);

Q8. What is the purpose of the typeof operator?

Q9. Create a variable student that stores: name, age, and isPresent (boolean).

Q10. Identify the datatype:

1) []
2) {}
3) "Hello"
4) true
5) undefined


⸻

⭐ 2. Operators (8 Questions)

Q11. What is the difference between == and ===?

Q12. What will be the output?

console.log(5 > 2 && 3 < 1);

Q13. What will this print and why?

console.log(!true);

Q14. Write a ternary operator to print "Adult" if age ≥ 18, otherwise "Minor".

Q15. Solve:

let x = 10;
x += 5;
console.log(x);

Q16. Identify the operator:

x >= 5  
x !== 10  
x *= 2  

Q17. Predict the output:

console.log(10 % 3);

Q18. Explain && and || in simple words.

⸻

⭐ 3. Loops – for…of & for…in (6 Questions)

Q19. Write a for…of loop to print each fruit:

const fruits = ["apple", "banana", "mango"];

Q20. Write a for…in loop to print keys and values:

const user = { name: "Raza", age: 25, city: "Mumbai" };

Q21. Why is for…in NOT recommended for arrays?

Q22. What is the output?

for (let char of "Khan") {
  console.log(char);
}

Q23. What is the output?

const obj = { a: 1, b: 2 };
for (let key in obj) console.log(key);

Q24. In one sentence: When do we use for…of, and when do we use for…in?

⸻

⭐ 4. Functions (6 Questions)

Q25. Write a normal function greet() that prints "Hello World".

Q26. Convert the same function into an arrow function.

Q27. What is a function expression? Give an example.

Q28. What will the following output?

function add(a, b) { return a + b; }
console.log(add(2, 3));

Q29. Why are functions extremely important in Node.js? List ANY 3 reasons.

Q30. Write a function that takes a number and returns its square.

⸻

⭐ 5. Arrays & Methods (12 Questions)

Q31. What is an array? How is it different from an object?

Q32. Write code to:
	•	Add an item to an array using push()
	•	Remove the last item using pop()

Q33. What will be the output?

const arr = [1, 2];
arr.unshift(0);
console.log(arr);

Q34. Explain: “Arrays are mutable even when declared with const.”

Q35. Write a for…of loop to print each number:

const nums = [10, 20, 30];

Q36. Write a forEach() loop to print each number.

Q37. Use .map() to double all values:

[1, 2, 3]

Q38. Use .filter() to get values greater than 20:

[10, 20, 30, 40]

Q39. Use .reduce() to calculate total:

[1, 2, 3, 4]

Q40. From the array below, find the user with age 26 using .find():

[
  {name: "Raza", age: 25},
  {name: "Khan", age: 26}
]

Q41. Use .some() to check if any number > 50:

[10, 20, 30]

Q42. Use .every() to check if all values are positive:

[1, 2, 3, 4]


⸻

🎉 Total Questions: 42

This set fully covers Phase 1 – JavaScript Basics and prepares students for:
	•	Callbacks
	•	Promises
	•	async/await
	•	Node.js modules
	•	Express.js
	•	MongoDB

⸻



